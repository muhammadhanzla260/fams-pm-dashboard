/** @type {import('next').NextConfig} */
const nextConfig = {
  // Emit a self-contained server bundle (.next/standalone) for easy server/Docker deploys.
  output: "standalone",
};

export default nextConfig;
